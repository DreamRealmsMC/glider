
cluster-settings:
  defaultGroup: auth
  fallback:
    fallbackIn: false # [true / false]
    # fallbackOut: [cluster name]
  last-location:
    enabled: false
    expireHours: 1


servers:
  Auth:
    registered: true
    maxPlayers: 100
    pterodactylHost: D1
    pterodactylID: auth0001

groups:
  auth:
    servers:
      - Auth
    settings:
      fallback:
        fallbackIn: false
      load-balancer:
        enabled: false
        optimalPlayers: 75
        minimumPlayers: 10
        loadOffset: 15